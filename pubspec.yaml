name: random_quote_app
description: Minimal Flutter app for showing a random quote.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.6.0
  share_plus: ^10.1.4

flutter:
  uses-material-design: true
